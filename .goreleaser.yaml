version: 2

project_name: comics-galore-library

# Skip building binaries for libraries
builds:
  - binary: ""
    goos: [ ]
    goarch: [ ]

archives:
  - format: tar.gz
    files:
      - "**/*.go"
      - "go.mod"
      - "go.sum"

release:
  github:
    owner: your-github-username
    name: your-repo-name

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
